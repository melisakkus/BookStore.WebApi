@model List<ResultCategoryDto>
@*
    _DefaultPopulerBooksComponent
*@
@{
    int count = 0;
    // int count2 = 0;
}
<section id="popular-books" class="bookshelf py-5 my-5">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="section-header align-center">
                    <h2 class="section-title">✴ En Sevilen Kitaplar ✴</h2>
                </div>

                <ul class="tabs">
                    @foreach (var category in Model)
                    {
                        count++;
                        <li data-tab-target="@category.CategoryId" class="@(count==1 ? "active" : " ") tab">@category.CategoryName</li>
                        @*
                    @foreach (var product in category.Products)
                    {
                    @product.ProductName
                    } *@
                    }
                </ul>
            </div>
        </div>
    </div>
</section>

@* <li data-tab-target="@category.CategoryId" class="@(count==1 ? "active" : " ") tab">@category.CategoryName</li>
 *@
@*                 <div class="tab-content">
                    @foreach (var category in Model)
                    {
                        count2++;
                        <div id="@category.CategoryId" data-tab-content class=@(count2==1 ? "active" : " ")>
                            <div class="row">
                                <div class="col-md-3">
                                    <div class="product-item">
                                        @foreach (var product in category.Products)
                                        {
                                            <figure class="product-style">
                                                <img src="@product.ImageUrl" alt="Books" class="product-item">
                                                <button type="button" class="add-to-cart" data-product-tile="add-to-cart">
                                                    İncele...
                                                </button>
                                            </figure>
                                            <figcaption>
                                                <h3>@product.ProductName</h3>
                                                <span>@product.AuthorName</span>
                                                <div class="item-price">₺ @product.ProductPrice</div>
                                            </figcaption>
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>
                *@